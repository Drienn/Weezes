import React from 'react';
/* eslint-disable */
import { storiesOf } from '@storybook/react';
import NotFound from './NotFound';

storiesOf('Pages/NotFound', module).add('Default', () => <NotFound />);
